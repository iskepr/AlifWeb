استورد واجهة
استورد الاذكار

قائمة_الاذكار = الاذكار.بيانات_الاذكار

اطبع(قائمة_الاذكار)
اطبع(طول(قائمة_الاذكار))
صفحة(العنوان:"الاذكار", الخلفية: "#111", اللون:"#fff"):
    توسيط():
        نص(النص:"موقع الاذكار بلغة برمجة الف")
        لاجل ذكر في مدى(طول(قائمة_الاذكار)):
            بطاقة():
                توسيط():
                    نص(النص: قائمة_الاذكار[ذكر])
                    بطاقة():
                        نص(النص: قائمة_الاذكار[ذكر])
                    صوت(المصدر:"https://github.com/rn0x/Adhkar-json/raw/refs/heads/main/audio/1.mp3")
